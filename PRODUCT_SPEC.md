# Smart Tab Cleaner - 产品说明书

## 概述

Smart Tab Cleaner 是一个 Chrome 侧边栏扩展，用于管理浏览器标签页。支持多窗口管理、多选操作、Tab Groups 等功能。

---

## 核心功能

### 1. 侧边栏标签列表

- 在浏览器侧边栏（Side Panel）中垂直显示所有标签页
- 按窗口分组显示
- 支持 Chrome 原生 Tab Groups 的显示和折叠
- 实时更新：标签创建、关闭、移动等操作会自动反映

### 2. 窗口管理

| 功能 | 操作 |
|------|------|
| 显示窗口名称 | 默认显示 "Current Window" 或 "Window {id}" |
| 自定义窗口名称 | **双击**窗口标题栏编辑名称 |
| 保存窗口名称 | 按 Enter 或失焦自动保存到 `chrome.storage.local` |
| 取消编辑 | 按 Escape |

### 3. 搜索

- 顶部搜索框支持按标题或 URL 过滤标签
- 实时过滤，输入即搜索

---

## 标签操作

### 单击操作

| 状态 | 操作 | 效果 |
|------|------|------|
| 无选中 | 单击标签 | 激活该标签并切换到对应窗口 |
| 无选中 | 单击关闭按钮 (×) | 关闭该标签 |
| 无选中 | 单击 Tab Group 标题 | 折叠/展开该分组 |
| **有选中** | 单击任意位置 | **只清除选择**，不触发其他操作（遮罩效果） |
| **有选中** | 单击关闭按钮 (×) | 仍可关闭该标签 |

### 多选操作

| 操作 | 效果 |
|------|------|
| **Cmd/Ctrl + 单击** | 切换该标签的选中状态（添加或移除） |
| **Shift + 单击** | 范围选择（从上次选中位置到当前位置的所有标签） |

**设计理念**：有选中状态时，普通点击相当于有一个透明遮罩，防止误操作。只有按住 Cmd/Ctrl/Shift 才能继续多选操作。

### 拖拽操作

| 操作 | 效果 |
|------|------|
| 拖拽标签到另一个标签上 | 移动到该位置 |
| 拖拽标签到另一个窗口区域 | 移动到该窗口末尾 |

---

## 右键菜单

### 单选时（选中 1 个标签）

```
🔄 Reload
📋 Duplicate
📌 Pin / Unpin
─────────────────
📁 Create Group
🪟 Move to new window
─────────────────
Close other tabs
✕ Close tab
```

### 多选时（选中 2+ 个标签）

```
[N tabs selected]
─────────────────
📁 Create Group        → 创建新分组（弹出输入框命名）
➕ Add to Group...     → 添加到已有分组（显示分组列表）
─────────────────
🔄 Reload All
📌 Pin All
🪟 Move to new window
─────────────────
✕ Close N tabs
```

### 右键菜单关闭逻辑

| 操作 | 效果 |
|------|------|
| 点击菜单项 | 执行操作后关闭菜单 |
| 左键点击侧边栏其他区域 | 关闭菜单，不触发其他操作 |
| 右键点击侧边栏其他区域 | 关闭菜单 |
| 按 Escape | 关闭菜单 |

**实现方式**：使用透明遮罩层（overlay）覆盖整个侧边栏，点击遮罩关闭菜单。

---

## Tab Groups 功能

### 创建分组

1. 选中一个或多个标签
2. 右键选择 "Create Group"
3. 在弹出的输入框中输入分组名称
4. 系统自动分配随机颜色

### 添加到已有分组

1. 选中一个或多个标签
2. 右键选择 "Add to Group..."
3. 从弹出的列表中选择目标分组

### 分组显示

- 分组以缩进 + 左侧彩色边框显示
- 点击分组标题可折叠/展开
- 显示分组内标签数量

---

## 会话管理

### 保存会话

点击顶部工具栏的 📚 Sessions 按钮：
1. 弹出会话管理面板
2. 点击 "💾 Save Current Session"
3. 输入会话名称
4. 系统保存当前所有窗口、标签、分组

### 保存内容

| 数据 | 说明 |
|------|------|
| 窗口 | 所有窗口及自定义名称 |
| Tab Groups | 分组名称、颜色、包含的标签 |
| 标签 | URL、标题、固定状态 |

### 恢复会话

1. 打开 Sessions 面板
2. 点击某个会话的 ▶️ 按钮
3. 确认后，系统创建新窗口并恢复所有标签和分组

### 其他操作

- 🗑️ 删除不需要的会话
- 最多保存 20 个会话
- `chrome://` 开头的系统页面不会被恢复

---

## 视觉设计

### 配色方案（深色主题）

```css
--bg-primary: #1e1e1e      /* 主背景 */
--bg-secondary: #252526    /* 次级背景 */
--bg-hover: #2a2d2e        /* 悬停背景 */
--bg-active: #37373d       /* 激活背景 */
--text-primary: #cccccc    /* 主文字 */
--text-secondary: #858585  /* 次级文字 */
--accent: #0078d4          /* 强调色（蓝） */
--danger: #f14c4c          /* 危险色（红） */
```

### 标签状态样式

| 状态 | 样式 |
|------|------|
| 默认 | 透明背景 |
| 悬停 | `--bg-hover` 背景 |
| 激活（当前标签） | `--bg-active` 背景，白色文字 |
| **选中（多选）** | 蓝色半透明背景 `rgba(0, 120, 212, 0.3)` |
| 固定（Pinned） | 带 📌 图标前缀 |

### Tab Group 颜色

支持 Chrome 原生的 9 种颜色：
`grey`, `blue`, `red`, `yellow`, `green`, `pink`, `purple`, `cyan`, `orange`

---

## 技术架构

```
browser_tab_cleaner/
├── manifest.json              # Manifest V3
├── icons/                     # PNG 图标 (16/48/128)
└── src/
    ├── background/
    │   └── service-worker.js  # 点击图标打开侧边栏
    └── sidepanel/
        ├── sidepanel.html     # 主界面
        ├── sidepanel.css      # 样式
        └── sidepanel.js       # 逻辑
```

### 使用的 Chrome API

| API | 用途 |
|-----|------|
| `chrome.tabs` | 标签页查询、操作 |
| `chrome.tabGroups` | Tab Groups 操作 |
| `chrome.windows` | 窗口操作 |
| `chrome.sidePanel` | 侧边栏管理 |
| `chrome.storage.local` | 存储窗口名称、会话数据 |

### 权限

```json
{
  "permissions": [
    "tabs",
    "tabGroups", 
    "storage",
    "sidePanel"
  ]
}
```

---

## 已知限制

1. **无法隐藏原生标签栏**：Chrome 不允许扩展控制浏览器 UI
2. **无法检测主窗口点击**：Side Panel 是独立上下文，`window.blur` 不触发
3. **无法修改窗口标题**：Chrome 没有 `chrome.windows.setTitle()` API

---

## 待开发功能

### 基础功能
- [ ] 批量拖拽（拖拽多选的标签）
- [ ] 键盘快捷键（Cmd+A 全选等）
- [ ] 编辑 Tab Group 名称和颜色
- [ ] 树形结构（父子标签关系）

### 高级功能（LLM）
- [ ] AI 智能分组
- [ ] 重复标签检测
- [ ] 陈旧标签建议
- [ ] 自动整理

---

## 版本历史

### v0.2.0 (当前)
- ✅ 会话保存/恢复

### v0.1.0
- ✅ 侧边栏标签列表
- ✅ 多窗口支持
- ✅ Tab Groups 显示
- ✅ 搜索过滤
- ✅ 拖拽排序和跨窗口移动
- ✅ 右键菜单（单选/多选）
- ✅ 多选操作（Cmd/Shift）
- ✅ 创建 Tab Group
- ✅ 自定义窗口名称

